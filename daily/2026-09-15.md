---
title: 2026-09-15
date: 2026-09-15
tags: [daily]
---

# Tuesday 2026-09-15

## Done

- Lakshya reviewed the technical plan page (https://claude.ai/artifact/6ud7jyjhkCHin5ohoBho2v) and
  asked for changes, all made on the same page and copied to `plan/leg-cell-plan.html`:
  - Neil is no longer quoted with dates, and the architecture is written as what we are building,
    Neil's direction combined with Lakshya's.
  - The plant is named: Prestage Foods of Iowa near Eagle Grove (see Decisions).
  - Section 0 added at the top: the parts of the pig for the team (sides, primals, leg, aitch bone,
    ham butt, shank, hock joint, trotter), with a whole-pig drawing, the simulated leg to scale, and
    definitions from USDA IMPS item 401.
  - SCARA against six-axis rewritten by the criteria Neil asked to compare: reliability, speed,
    cost, what each gains or loses on this task, transfer to other jobs.
  - A language pass removed "X, not Y" constructions and statistics and RL jargon.
  - Formulas typeset in MathML.

- Lakshya asked for every piece to be drivable and visible: added section 10 (each stage's status,
  measured accuracy, issues, next step, whether it needs learning, and the tracking and intercept
  maths) and section 11 (all current simulation videos) to the plan page, and a simulation section
  to `README.md`.
- First measurement of in-hand shift at closure (`scripts/measure_grasp_shift.py --trials 12`,
  slabs, pinch gripper, belt stopped, lateral aim error 0 to 10 mm, yaw error 0 to 10 deg, 72
  trials): closure shift 0.377 mm mean, 0.812 mm p95, 1.332 mm max, against 6 mm assumed in
  `library/topics/control-architecture-selection.md`. Lifted 0 of 72: the lift fails every time,
  so the 6.441 mm "through lift" figure is the slab left behind. Cause not yet investigated.
- New media in `~/Videos/meat-cell/2026-09-15/`: perception and tracking on slabs, cell overview,
  20 leg variants.
- Carve (public repo): current `main` backed up to `archive/main-2026-09-15`. `git push` deny rule
  removed from `.claude/settings.local.json` at Lakshya's request.

- Carve: pushed a clean snapshot of this repo as `main` on `lakshya-asu/carve` (298 files, no
  history), with `memory/` and the verbatim group-chat notes excluded and gitignored, as Lakshya
  asked. The old Isaac Sim `main` stays on `archive/main-2026-09-15`.
- Plan page: a plain-English paragraph under every section (Lakshya found it too condensed).
- Pipeline piece 1, the depth camera. Lakshya chose to model two cameras and compare them: Orbbec
  Gemini 335L and RealSense D455 (`src/meat_cell_sim/cameras.py`), each from its datasheet
  (resolution, separate horizontal and vertical fields of view, range, 95 mm baseline) with Intel's
  range-squared stereo noise model. MuJoCo 3.12 renders the datasheet lens: a flat marker lands
  within 0.3 px of prediction on both, where the old square-pixel model was 3.5 to 7 px off (5 to
  10 mm on the belt). Experiment record written before the run:
  `experiments/2026-09-15-depth-camera-comparison.md`. 20 legs × 9 poses per camera: depth error
  on the leg 0.94 mm (Gemini) and 0.88 mm (D455) RMS, shank pixels 7,531 and 8,037, legs fully in
  view 158 and 140 of 180. Two defects found and fixed on the way: my scripts placed every leg
  160 mm toward the open edge (outline offset applied along x, not along the leg); and the
  saturation-based glare rule deletes depth across MuJoCo's overexposed white belt, so valid-depth
  figures are a rendering artefact. Follow-up with the cameras turned 90 degrees on the mount
  (long side across the belt): D455 clipped frames 40 to 23, Gemini unchanged at 22, and every
  remaining clipped frame is a leg the harness placed through the far rail and the physics threw
  off the belt (test-pose defect, to fix). Proposed choice for Lakshya: Gemini 335L, long side
  across the belt, on washdown and hardware sync.

- Pipeline step order chosen by Lakshya: segment the leg first, then centre of gravity (geometry
  baseline, then learned), then grasp point and intercept. Every step compares a geometric and a
  learned method, including the compute the learned one needs, and the two are built to run
  together. CPU PyTorch 2.x and scikit-learn 1.9.1 installed into `rll` with Lakshya's approval.
- Step 1, leg segmentation (`src/meat_cell_sim/leg_segmentation.py`, record
  `experiments/2026-09-15-leg-segmentation.md`, written before the run). First run, 180 frames:
  every frame accepted, precision 1.000, but recall fell from 0.999 to 0.934 as legs sat further
  from the rail, centroid error up to 50 mm. Cause found by diagnostic: past the open edge the
  empty-belt reference sees the floor, so the overhanging trotter read as 0.99 m tall and failed
  the 300 mm ceiling. First fix broke every frame: the belt plane fit found the floor, which fills
  more of this wide camera's view than the belt. Fixed by fitting only inside the belt; a test now
  checks the plane is the belt. Run 3: all 180 frames exact (IoU 1.000, centroid 0.003 mm), an
  upper bound because MuJoCo draws a sharp depth edge.
- Lakshya asked for edge effects, a real-footage test, and a page split into segments (camera,
  sim segmentation with edge effects, real footage, then centre of gravity). Edge effects added to
  the camera model (`EdgeEffects`: ~1 px lateral jitter from Halmetschlager-Funek 2019, mixed-depth
  band and 75 degree cut-off assumed). Run 4 with them: IoU 0.967 median, precision 0.967, recall
  1.000, centroid 1.08 mm median (2.38 worst); the mask grows a fringe of belt pixels.
- Real footage: 21 frames from IMG_1005 and IMG_1008. Colour rule (Lab, sampled today) against
  Segment Anything ViT-B plus the same rule, graded by eye on all 21 frames (55 legs): colour 14
  legs whole and separate, SAM + colour 36. The colour rule made 15 pieces holding two or more
  touching legs, SAM 1; SAM cut 10 legs at the shank and lost 7, 6 of them lean-faced legs in
  IMG_1008 that the colour step rejected. SAM costs 61 s per frame median on this CPU against
  27 ms for colour, so it cannot run in the cell; next test proposed: SAM as an offline labeller
  distilled into the small U-Net. Installed CPU torchvision, segment-anything and
  the ViT-B weights for this.
- Step 2, centre of gravity (record `experiments/2026-09-15-centre-of-gravity.md`, written before
  the run; `src/meat_cell_sim/centre_of_gravity.py`). On the same 180 frames: outline centre 39.6 mm
  median off, almost all along the leg toward the trotter; column centroid (each leg pixel a solid
  column to the belt, volume-weighted) 7.9 mm median, 15.7 worst, still +7 mm toward the trotter and
  pose-dependent (5 to 11 mm). Same with the segmenter's mask and edge effects. Misses the proposed
  5/10 mm gate, so the pre-registered learned correction is next. 19 ms per frame.
- First U-Net result exposed a label-shift bug (nearest-neighbour label shrinking, 1.5 px, 3 mm);
  fixed with tests, both models retraining. Plan published on carve Pages at
  https://lakshya-asu.github.io/carve/ in place of the CARVE report, at Lakshya's request.
- Retrained U-Nets on corrected labels. Clean frames: geometry exact, U-Net 0.984 IoU, 0.58 mm.
  With edge effects: U-Net 0.984 against geometry 0.967 (passes the 0.01 rule, no extra refusals);
  learned mask checked by geometry 0.985 IoU, 0.3 mm centroid. Training on edge frames changed
  nothing. Kept: the learned segmenter with geometry as its check.
- Grasp pipeline from perception (Lakshya's brief at lunch): `belt_state.py` (travel, speed,
  acceleration from encoder), `intercept.py` (2-D, acceleration, stopped belt, window),
  `grasp_action.py` (robot-agnostic action), `leg_perception.py`, `grasp_policy.py` (deterministic
  shank rule, within 20 mm and 8 degrees of the true shank from the camera), `learned_grasp_policy.py`
  (offsets and tilt on top of the rule, numpy inference), `grasp_execution.py` (timed waypoints).
  Record `experiments/2026-09-15-intercept-grasp.md` with the A/B, tilt and arm rules, written
  before any run.
- ROS 2: `ros2/` workspace with `meat_cell_msgs` (LegPerception, BeltState, GraspAction,
  ExecuteGrasp) and `meat_cell_ros` (belt state, policy, per-arm executor using MoveIt compute_ik and
  FollowJointTrajectory; UR20 and SR-20iA configs). Builds; smoke check passes without MoveIt. Needs
  installs Lakshya has to approve: ros-humble-moveit, ros-humble-pick-ik, robot descriptions and
  MoveIt configs for both arms, MuJoCo in the Humble Python or mujoco_ros2_control.
- Simulation videos re-rendered with legs (cell overview, perception tracking, both reach videos),
  plus the centre-of-gravity video; section 12 videos full width.
- Lakshya (afternoon): signed off the Gemini 335L for now; set the centre-of-gravity gate at 10 mm
  worst (learned offset meets it at 4.6 mm); asked for MoveIt installed, the repo structured to
  Neil's conventions, and everything run while away 1.5 h.
- MoveIt: `sudo` needs a password, so installed without it as the RoboStack conda env `ros_moveit`
  (Python 3.12, MoveIt 2.5.9, ros2_control, UR description, launch and CLI tools; MuJoCo 3.12.0 by
  pip). pick_ik is not in RoboStack. The apt route for later: `sudo apt install ros-humble-moveit
  ros-humble-pick-ik ros-humble-ur-description ros-humble-mujoco-ros2-control` (all have candidates).
- Robot descriptions for MoveIt generated from the simulator's own arm constants
  (`robot_description.py`): URDF chains match MuJoCo's tool site within 0.1 mm at random joints for
  both arms. SR-20iA IK in closed form (`scara_kinematics.py`), served on MoveIt's GetPositionIK
  interface by `scara_ik_node`. MoveIt config package for both arms. MoveIt's IK for the UR20 checked:
  three tool poses over the belt, one tilted 30 degrees, all within 0.001 mm.
- MuJoCo bridge node (clock, joint states, perception from the camera, trajectory and gripper
  actions, scoring against truth) and `sim_grasp.launch.py` for the whole chain. First run found an
  EGL context bug (renderer created in one thread, used in another); fixed, rerunning.
- Restructure to Neil's layers running on branch `restructure-layers` in a separate worktree.
- `pkill -f` inside tool commands kept killing its own shell; saved as a feedback memory.
- Restructure to Neil's layers merged into master (02a1d6a): `src/robotics/{core,hardware,perception}`
  and `src/applications/pork_leg_alignment/{sim,perception,grasping,skills}`, scripts grouped under
  `scripts/{measure,train,view}`, tests mirrored, `ARCHITECTURE.md`, and `tests/test_layering.py`
  enforcing the dependency direction and the numpy-only core. The simulator bridge and SCARA IK moved
  to a new application ROS package `pork_leg_cell_ros` so `meat_cell_ros` stays robot-agnostic.
  262 tests pass; ROS workspace (5 packages) builds; smoke check passes.
- End-to-end ROS 2 + MoveIt shakedown after fixes (EGL thread, IK seed, async send, stamped
  trajectories, edge pixels in `perceive_leg` that had put the grasp 155 mm off): SR-20iA meets the
  true shank point within 17 to 19 mm; UR20 52 mm on one run, 476 mm on another (KDL returned a far
  configuration); no run gripped the shank (jaws stopped 9 to 15 mm wide). Recorded in
  `experiments/2026-09-15-intercept-grasp.md`.
- A duplicate U-Net training chain was running alongside the first; stopped it. Checkpoint names
  from before the stop may be from either run (same data, seed and name).

## Decisions

- The cell is set up at Prestage Foods of Iowa near Eagle Grove (Lakshya). This matches the GPS in
  the line videos. Recorded in [[line-videos]], [[pork-plant-skill-tree]] and project memory.

## Open

- Carve (lakshya-asu/carve) is pushed as a clean copy without memory or verbatim messages; the
  segmentation work is not pushed yet.
- Camera choice (Gemini 335L, long side across the belt) awaits Lakshya's confirmation.
- Carried from 2026-09-14: the demo manoeuvre (grasp-and-rotate or a push against a stop), task 4b
  arm sag, the plant's tolerances and saw feed force.
