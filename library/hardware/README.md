# Hardware

One file per robot, sensor, or compute platform met in the field. Include:
model, firmware, interface (ROS driver, SDK), control rate, known quirks,
who to contact, links to manuals.

Known so far: ROBOTIS AI Worker with ZED 2i camera and Nav2 (from prior demo work).
