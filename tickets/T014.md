---
id: T014
status: done
priority: P1
depends_on: [T005, T006, T007, T008, T009, T010, T011, T012, T013]
---

# Integration tests and overnight report

Integrate completed tickets inside Isaac Sim, run the full test set, and produce `OVERNIGHT_REPORT.md`.

## Acceptance

- A visible saved USD contains the full reference cell, required assets, sensors, guards, stations, and named frames.
- A real Isaac articulation executes bounded interception, grasp, transfer, alignment, feed, retract, and recovery trajectories.
- Rendered RGB and depth drive the primary replaceable perception interface with noise and latency injection.
- Conveyor physics affects observations and timing. Two-finger contact is required before simulated attachment.
- The complete state machine and PLC interface execute against simulator state.
- A and B each run multiple seeded cycles and save media, traces, metrics, and stages.
- Automated gates cover stage contents, sensors, calibration, controller execution, grasp success and failure, alignment, timing, collisions, limits, recovery, and replay.
- A and B each perceive, track, intercept, contact-grasp, reorient, and deliver at least one workpiece.
- Ticket statuses match actual evidence and the overnight report records commands, results, assumptions, and blockers.

## Evidence

- `python -m pytest -q`
- `C:\Users\jainl\is6\Scripts\python.exe isaac_sim\validate_setup.py --headless`
- `C:\Users\jainl\is6\Scripts\python.exe isaac_sim\run_cell.py --solution a --cycles 4 --seed 7 --headless`
- `C:\Users\jainl\is6\Scripts\python.exe isaac_sim\run_cell.py --solution b --cycles 4 --seed 7 --headless`
- Result: unit suite, setup validation, and every integrated gate passed on 2026-08-26. Machine evidence is in `results/setup_validation.json`, `results/isaac_a/metrics.json`, and `results/isaac_b/metrics.json`.

## 2026-08-26 hardening evidence

- `.\run_tests.ps1`: passed from a clean simulator process state. Current result is 77 unit tests passed, 1 skipped, setup passed, A passed, B passed, and the baseline artifact audit passed.
- `.\run_hardening.ps1`: ran solutions A and B at seeds 7, 31, 101, 509, and 1001 with six cycles per batch.
- `.\run_hardening.ps1 -Resume`: completed the seed 31 retry after the safe-stop hold target was clamped to declared joint limits.
- `results/hardening/summary.json`: 10 of 10 batches passed, 60 cycles audited, 20 nominal deliveries succeeded, 40 injected failure or safe-stop paths produced their expected outcomes, 60 replay checks passed, and no unexpected collision or joint-limit violation was recorded.
- The independent audit checked 10 saved USD stages, 10 RGB images, 10 depth arrays, and 60 JSON Lines traces. It also checked rendered segmentation evidence, articulation commands, contact-confirmed grasps, product-frame alignment, PLC transitions, failed grasp, cutter unavailable, buffer timeout, slip correction, emergency stop, stale observation, and recovery.

## 2026-08-26 learned-vision evidence

- Ultralytics YOLO26 nano segmentation was fine-tuned on 240 Isaac-rendered synthetic frames. Ground-truth geometry was used for training labels only.
- The v2 synthetic holdout passed at mask mAP50 0.9188 and mask mAP50-95 0.6923.
- Solution A passed four learned-vision cycles with two nominal deliveries, failed-grasp recovery, cutter-unavailable reject, 5,591 articulation commands, zero collisions, zero joint-limit violations, and four replay checks.
- Solution B passed four learned-vision cycles with two nominal buffered deliveries including slip correction, failed-grasp recovery, buffer-timeout recovery, 6,603 articulation commands, zero collisions, zero joint-limit violations, and four replay checks.
- Machine evidence is in `results/yolo/training_summary.json`, `results/yolo/headless_a_v2/isaac_a/metrics.json`, and `results/yolo/headless_b_v2/isaac_b/metrics.json`.
